//! The mirrored rival AI: paced think tick (build order → worker assignment → training →
//! attack waves) spending from its own bank under identical rules.

use bevy::prelude::*;

pub struct RtsAiPlugin;

impl Plugin for RtsAiPlugin {
    fn build(&self, _app: &mut App) {}
}
