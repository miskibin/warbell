//! Unit/building selection: LMB click, drag box, Shift add; selection rings + outline.

use bevy::prelude::*;

pub struct RtsSelectPlugin;

impl Plugin for RtsSelectPlugin {
    fn build(&self, _app: &mut App) {}
}
