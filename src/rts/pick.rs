//! Cursor→world picking for the RTS mode: viewport ray vs terrain, unit/building hit tests.

use bevy::prelude::*;

pub struct RtsPickPlugin;

impl Plugin for RtsPickPlugin {
    fn build(&self, _app: &mut App) {}
}
